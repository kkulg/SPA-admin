const state = (): any => ({
  // 当前用户信息
  user: null,
  // 左侧边栏菜单
  asideMenus: []
});

const getters = {};

const mutations = {};

const actions = {};

export default {
  state,
  getters,
  mutations,
  actions
};
